# Idempotent local bootstrap to map Lambda role to OpenSearch all_access after domain (re)creation
# (Removed) Local bootstrap provisioner is not needed for VPC-only domain because
# the EC2 instance user_data already performs the rolesmapping from inside the VPC.
# Keeping this file as a placeholder to avoid confusion / accidental recreation.
